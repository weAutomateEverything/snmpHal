version: 0.2

phases:
  install:
    commands:
    # printenv
    # pwd
    - mkdir -p /go/src/github.com/weAutomateEverything/snmpHal
    - cp -R * /go/src/github.com/weAutomateEverything/snmpHal/
  pre_build:
    commands:

    finally:

  build:
    commands:
    - go get github.com/weAutomateEverything/snmpHal
    - GOOS=linux go build -ldflags '-linkmode=external' -o ./snmpHal github.com/weAutomateEverything/snmpHal
    -
  post_build:
    commands:
    finally:

artifacts:
  files:
  - snmpHal
cache:
  paths:
